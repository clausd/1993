describe("1993", function() {
  beforeEach(function() {
    // something
  });

  it("should save element on save", function() {
    // load a fixture
    expect(typeof $('div').save('tosomeurl')).toBeTruthy();
  });

});